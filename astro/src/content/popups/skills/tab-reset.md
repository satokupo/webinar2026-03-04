# tab-reset（タブリセット）

## 何のためのスキルか

作業が完了したタブを「待機状態」に戻すスキル。tmux 環境前提。
タブ名をデフォルト（動的表示）に戻し、コンテキストをクリアして次の作業にすぐ使える状態にする。

save-chatlog の最終ステップで、ユーザーが選択した場合に呼ばれる。

## やっていること

1. tmux のウィンドウ名を automatic-rename（動的表示）に復元
2. /clear でコンテキストウィンドウをクリア

## 工夫したこと

特になし
